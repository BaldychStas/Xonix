using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum CellType
{
    Enemy = -1,
    Water = 0,
    Ground = 1,
    PlayerWall = 2,
}

public enum Direction
{
    Up,
    Right,
    Down,
    Left
}

